/* Published gallery configuration (shared by all visitors, never localStorage).
   Add only real, approved public videos, for example:
   { title: 'Installation walkthrough', url: 'https://…', poster: 'assets/images/…' }
   No video is displayed or advertised until one is supplied here. Alternatively,
   set the proposal's Gallery URL to an existing public video gallery / playlist. */
window.GALLERY_VIDEOS = [];
